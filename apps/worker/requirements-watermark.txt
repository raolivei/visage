# Minimal requirements for watermark removal worker (CPU-only)

# PyTorch CPU-only (smaller footprint)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0
torchvision>=0.16.0

# Watermark removal (LaMa inpainting)
simple-lama-inpainting>=0.1.0

# Image processing
Pillow>=10.2.0
opencv-python-headless>=4.9.0
numpy>=1.26.0
scipy>=1.11.0

# S3/MinIO
boto3>=1.34.0
botocore>=1.34.0

# Redis
redis>=5.0.0

# HTTP client (for API calls)
httpx>=0.26.0

# Utils
python-dotenv>=1.0.0
pydantic>=2.6.0
pydantic-settings>=2.1.0

# Progress and logging
tqdm>=4.66.0
rich>=13.7.0

# Metrics
prometheus-client>=0.19.0
