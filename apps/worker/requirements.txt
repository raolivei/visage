# PyTorch (MPS-compatible)
torch>=2.1.0
torchvision>=0.16.0

# Diffusers and transformers
diffusers>=0.25.0
transformers>=4.36.0
accelerate>=0.25.0
safetensors>=0.4.0

# LoRA training
peft>=0.7.0

# Image processing
Pillow>=10.2.0
opencv-python>=4.9.0
numpy>=1.26.0

# Face detection/recognition (InsightFace is preferred)
insightface>=0.7.3
onnxruntime>=1.16.0

# Background removal (optional)
rembg>=2.0.50

# Watermark removal (LaMa inpainting)
simple-lama-inpainting>=0.1.0

# Quality scoring - CLIP aesthetic
# Note: CLIP requires special install - see README
# pip install git+https://github.com/openai/CLIP.git

# Post-processing
# codeformer-perceptor>=0.1.0  # Install separately due to dependencies
# realesrgan>=0.3.0  # Install separately
basicsr>=1.4.2

# S3/MinIO
boto3>=1.34.0
botocore>=1.34.0

# Redis
redis>=5.0.0

# HTTP client (for API calls)
httpx>=0.26.0

# Utils
python-dotenv>=1.0.0
pydantic>=2.6.0
pydantic-settings>=2.1.0

# Progress and logging
tqdm>=4.66.0
rich>=13.7.0

# Metrics
prometheus-client>=0.19.0

# Development
pytest>=8.0.0
ruff>=0.2.0
